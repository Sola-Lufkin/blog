// Language switching is handled via inline onclick + global langSwitch() in post.html
// No jQuery click binding here to avoid double-firing with onclick attribute.
$(document).ready(function() {
});
